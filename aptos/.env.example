# Aptos Indexer Configuration
# Most values have sensible defaults - only change if needed

# ====== Network Selection ======
# Options: mainnet, testnet, devnet
APTOS_NETWORK=mainnet

# Docker image tag (automatically set by setup.sh based on network)
IMAGE_TAG=mainnet
VALIDATOR_IMAGE_REPO=aptoslabs/validator

# ====== Database Configuration ======
# These will be auto-generated with secure values if using defaults
POSTGRES_USER=postgres
POSTGRES_PASSWORD=indexer_password
POSTGRES_DB=aptos_indexer
# Port 5433 used to avoid conflict with Soroban indexer (which uses 5432)
POSTGRES_PORT=5433

# ====== Hasura GraphQL Configuration ======
# Admin secret will be auto-generated if using default
HASURA_ADMIN_SECRET=hasura_admin_secret
HASURA_PORT=8090  # Changed from 8080 to avoid conflict with fullnode

# ====== Full Node Ports ======
# Only change if you have port conflicts
FULLNODE_API_PORT=8082
FULLNODE_METRICS_PORT=6182
FULLNODE_INSPECTION_PORT=9101
FULLNODE_GRPC_PORT=50051

# ====== Optional: External Transaction Stream ======
# Only needed if NOT running local full node
# Get token from https://developer.aptoslabs.com
# APTOS_AUTH_TOKEN=your_token_here
# EXTERNAL_GRPC_ENDPOINT=https://grpc.mainnet.aptoslabs.com:443
